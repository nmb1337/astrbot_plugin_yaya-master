{
  "group_whitelist": {
    "description": "群聊白名单",
    "type": "text",
    "hint": "每行填写一个群号，仅白名单内的群聊会触发此插件。留空则对所有群聊生效",
    "default": ""
  },
  "use_ai_reply": {
    "description": "触发关键词时是否同时使用 AI 大模型回复",
    "type": "bool",
    "hint": "开启后，关键词匹配时会同时触发 AI 大模型的回复；关闭后仅发送关键词配置的回复内容",
    "default": false
  }
}
